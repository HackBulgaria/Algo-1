class BandwidthManager:

#receives a packet with specified protocol and payload
  def rcv(protocol, payload):
      pass

#returns the payload of the packet which should be sent
  def send():
      return "payload"
